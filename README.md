# concert-predictor
Data analysis using python; GUI using Gradio; Web scraping using Selenium

- python version must be 3.8 or 3.9 bc wheel
- must run `pip install --upgrade urllib3==1.26.16` since chrome versions want to be weird